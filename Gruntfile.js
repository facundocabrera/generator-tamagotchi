'use strict';

module.exports = function(grunt) {
  // grunt.initConfig({
  //   release: {
  //     options: {
  //       npmtag: true,
  //     }
  //   }
  // });

  grunt.loadNpmTasks('grunt-release');
};
